services:
  app:
    build: .
    container_name: ms-notificaciones-app
    ports:
      - "8085:8085"
    env_file:
      - .env
    networks:
      - network-RedNorte

networks:
  network-RedNorte:
    external: true
